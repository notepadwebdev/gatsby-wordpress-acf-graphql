import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import FlexibleBlocks from './FlexibleBlocks';
import * as ContentBlockStories from '../ContentBlock/ContentBlock.stories.mdx';

<Meta title="Organisms/FlexibleBlocks" component={FlexibleBlocks} />

export const Template = args => <FlexibleBlocks {...args} />;

export const Default = Template.bind({});
Default.args = {
  blocks: [
    {
      ...ContentBlockStories.Default.args,
      title: `Block Title #1`,
      fieldGroupName: `page_Flexibleblocks_FlexibleBlocks_FbContentBlock`,
    },
    {
      ...ContentBlockStories.Default.args,
      alignment: `image-right`,
      title: `Block Title #2`,
      fieldGroupName: `page_Flexibleblocks_FlexibleBlocks_FbContentBlock`,
    }
  ]
}

# FlexibleBlocks

A container area for adding multiple ContentBlocks to a page or post.

<ArgsTable of={FlexibleBlocks} />

<Canvas>
  <Story name="Default" args={Default.args}>
    {Template.bind({})}
  </Story>
</Canvas>
